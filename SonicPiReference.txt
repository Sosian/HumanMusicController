live_loop :synth do
  use_real_time
  use_synth :fm
  a = sync "/osc*/note/fm"
  play a, release: 5
  sleep 1
end

Speed change:
loop do
  use_real_time
  a = sync "/osc*/speed"
  set(:bpm, a[0])
end

live_loop :drum1 do
  play 50
  
  use_bpm get(:bpm)
  sleep 1
end


Synth:
use_synth :dark_ambience
dark = play 80, sustain: 5000

use_synth :growl
growl = play 80, sustain: 5000

live_loop :test do
  use_real_time
  control dark, note: get("/osc*/note/dark_ambience")[0]
  sleep 0.2
end

live_loop :test do
  use_real_time
  control growl, note: get("/osc*/note/growl")[0]
  sleep 0.2
end

live_loop :test do
  use_real_time
  use_synth :piano
  play get("/osc*/note/piano")[0]
  sleep 0.2
end